//#include "libft.h"
#include <stddef.h>

size_t  strlcat(char *dst, const char *src, size_t size)
{
	int i;
	int j;
	int	dest_len;

	i = 0;
	j = 0;
	dest_len = ft_strlen(dest);
	if (size <= 0)
		return (dest_len)
	//TODO a finir
	
}